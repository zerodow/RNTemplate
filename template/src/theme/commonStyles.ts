import { StyleSheet } from "react-native"

export const commonStyles = StyleSheet.create({
  center: {
    alignItems: "center",
    justifyContent: "center",
  },
  flex1: {
    flex: 1,
  },
  flexCenter: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
})
